<?xml version="1.0" encoding="utf-8"?>

<!--
    AndroidManifest.xml
    Manifest file for GradeNet
    By Nicholas Verrochi
    Last Modifieid: 5/7/18
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bhcc.nick.gradenet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AdminMenuActivity" />

        <activity android:name=".StudentListActivity" />

        <activity android:name=".StudentDetailActivity" />

        <activity android:name=".TeacherListActivity" />

        <activity android:name=".TeacherDetailActivity" />

        <activity android:name=".CourseListActivity" />

        <activity android:name=".CourseDetailActivity" />

        <activity android:name=".AdminListActivity" />

        <activity android:name=".AdminDetailActivity" />

        <activity android:name=".CourseStudentListActivity" />

        <activity android:name=".CourseStudentDetailActivity" />

        <activity android:name=".TeacherMenuActivity" />

    </application>

</manifest>